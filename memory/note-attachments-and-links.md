- **2026-06-20** — Excalidraw editor default settings + anchor fix. **Changes:** (1) `initialData.appState` in `DrawingEditor.tsx` now sets `currentItemFontFamily: FONT_FAMILY.Nunito`, `currentItemRoughness: 0` (architect), `currentItemStrokeWidth: 1` (thin), `currentItemRoundness: 'sharp'`. Added `FONT_FAMILY` import from `@excalidraw/excalidraw`. (2) `syncViewportOffsets` effect no longer guards on `!usesMobileEditorLayout` — the effect now runs on all device types so that `offsetTop`/`offsetLeft` are correctly set before the first pointer interaction, fixing the "anchor appears at center" bug on desktop. **Verify:** new drawing → text tool default is Nunito; new rectangle/arrow → thin stroke, no roughness, sharp corners. On both mobile and desktop, clicking to start a line/rectangle creates the anchor exactly where the pointer is, not at screen center. **Do not reintroduce:** the `!usesMobileEditorLayout` guard in the offset sync effect — it caused desktop pointer coordinates to be miscalculated.

- **DEFERRED (needs production server test) — 2026-06-20:** Drawing thumbnails on media-only note cards can be slow on the first 1–2 cold starts after a full cache wipe + login. Observed inconsistently (slow on 2 of 3 tests, instant on the 3rd). Likely causes: `peekLatestDrawingThumbnail` reads from `localStorage` (`freemannotes.latest-drawing-thumbnails.v1`) which is only populated after the first successful render → first session has nothing to peek. After that session, subsequent warm starts are instant. May also be affected by PWA service worker cache state, Android OS, or dev server vs. production CDN. **Do not fix until tested on a real production server build.** Candidate fix if confirmed: pre-warm the drawing thumbnail localStorage snapshot from IDB in `main.tsx` before `createRoot()`, similar to `preloadWarmStartupImages()`.

- Attachment UI now routes through NoteAttachmentCountChip with three browser kinds: images, links, documents.
- Document attachments use DocumentsPanel + noteDocumentStore + NoteDocumentViewer with format-aware previews: PDF via react-pdf, DOCX via cached Mammoth HTML in a sandboxed iframe, XLS/XLSX/ODS via client-side xlsx tables, and other office docs via server-side PDF conversion plus a lazy-loaded fallback viewer.
- noteDocumentStore now caches derived HTML/PDF render outputs in IndexedDB so already-opened document previews still work offline after the original file blob is cached.
- noteDocumentStore refreshes should not emit note-documents-changed from internal cache/blob sync paths; those events can cause attachment chip and documents panel refresh loops with heavy IndexedDB traffic.
- On coarse pointers, document viewer modals must opt out of the global body-scroll-locked touch-action:none rule or mobile pinch/scroll will be blocked even if useBodyScrollLock is called with disableTouchAction:false.
- Link previews derive canonical URLs from Yjs metadata (noteLinks.ts) and sync through noteLinkStore/noteLinkPreview.js with @extractus/article-extractor as fallback.
- Release validation for this stack: npm run build succeeds; existing Vite chunk-size warnings remain non-blocking.
- Warm-boot link rails should not rely on the in-memory noteLinkStore cache alone. Persist a compact resolved preview-card shell (title/description/image URL) in the workspace render snapshot and feed it into NoteLinkPanel on first paint, otherwise the rail briefly falls back to the generic link icon until IndexedDB hydration finishes.
- Checklist note cards on Android/webview paint disabled native checkboxes with a visibly different shell during snapshot boot. Use a deterministic custom checkbox style in NoteCard so snapshot and live phases do not swap control visuals even when editability changes.


- Mobile photo capture in NoteImageUploadModal must immediately downscale camera files to 1280px max and recompress to JPEG via canvas.toBlob before preview or queueing; keep processed Files in refs, never raw camera Files or base64 in React state.
- Attached drawings are stored as raw child note ids in the parent note doc. Shared or moved notes must resolve those drawing ids through the parent note's current room/workspace and register a temporary external alias before loading/opening the drawing, or collaborators open an empty sample drawing from the wrong room.
- Editor-side image/document/link panels should use the same resolved room/docId source as note-card chips (`DocumentManager.resolveRoomName(...)` for the selected note). If the editor recomputes docIds separately, shared and moved notes can show chips correctly while the media panel queries an empty room.
- Pinning is user-scoped state now; App/NoteGrid actions must go through notePinPreferences helpers instead of mutating shared note metadata, or collaborators inherit each other's pinned state.

- Offline URL preview recovery must not stop at `status === 'PENDING'`. The server considers link hydration incomplete when a row is non-FAILED but still lacks meaningful text or a usable image, so both reconnect scans (`noteLinkStore.scanAllDocumentsForPlaceholders`) and `NoteLinkPanel` retries need that broader rule or previews can stay blank after coming back online.

- Note-card discoverability: on coarse pointers, long-press should belong to grid drag rather than the card more-menu. `NoteGrid` now gates touch drag start on the existing non-interactive `pendingTouchIntentRef` path and `NoteCard` exposes a persistent bottom-right ellipsis button instead of long-press-to-menu. Keep that button above the link preview rail with subtle theme-aware colors; do not persistently hide the more-menu entry inside the hover-only footer dock.

- 2026-06-14 — Untitled text/checklist cards with no body content rendered blank in the grid even when the media panel held drawings or images. **Symptom:** empty-looking cards despite attachment chips. **Fix:** `NoteCard` detects media-only empty notes and shows the first linked drawing thumbnail (same layout as drawing notes) or up to four cropped image tiles in a 2×2 grid; drawings take priority over images. Hydrate image URLs from IDB/cache first and refresh in the background (`noteMediaStore`).
- 2026-06-14 — Media-only image previews still missing on cards. **Cause:** empty TipTap docs render a placeholder `<br />` paragraph so `renderRichPreview !== null` blocked the media-only gate; queued/offline image blobs were not merged into card hydration. **Fix:** treat content empty when extracted plain text is empty; include queued uploads + preview blobs in `useNoteCardImages`.
- 2026-06-14 — Saving a new attached drawing closed the whole editor instead of returning to the parent draft. **Cause:** `savePendingNewNoteAndClose` always dismissed the overlay; desktop has no overlay history back to parent. **Fix:** `saveDrawingEditor` / `closeNoteEditor` reopen the parent note via `openNoteEditor(parentId, { replaceTop: true })` when `pendingAttachedDrawingParentsRef` maps the drawing id.
- 2026-06-14 — Narrow desktop cards (<380px) overlapped media image thumbnails. **Cause:** global `max-height` + flex-shrink squashed the 2×2 `aspect-ratio` grid. **Fix:** `mediaPreviewCard` lifts max-height; grid cells use padding-bottom squares instead of aspect-ratio.
- 2026-06-15 — Cross-column drag on media-only image cards flashed blank previews. **Cause:** grid drag ghost cloned `innerHTML` once; cross-column preview columns remount the card in another `VirtualizedNoteColumn`, revoking blob URLs while the stale markup ghost still referenced them. **Fix:** capture markup synchronously in `beginManagedDrag`; retain blob URLs via `noteCardDragMediaRetention.ts` until drop-settle completes; use markup ghost (not live `NoteCard`) for media previews.
- 2026-06-15 — Media/drawing cards flashed at drag pickup. **Cause:** ghost briefly fell back to a live `NoteCard` (async media reload). **Fix:** synchronous `dragPreviewMarkupRef` + always use markup ghost; retain blob URLs via `noteCardDragMediaRetention.ts` during cross-column remounts.
- **2026-06-17** — Offline pending-new note: image viewer blank + no attachment chip after save. **Symptom:** mobile offline create note → add images → tap tile shows "image unavailable offline"; after save, card has no paperclip chip. **Cause:** `useResolvedNoteImageSource` skipped `blob:` preview URLs when offline; `NoteGrid` `renderNoteMetaChips` early-returned when `liveAttachmentTotal <= 0`. **Fix:** treat `blob:` URLs as immediately usable; pass queued `fallbackThumbnailBlob`; mount meta rail when `docId` exists. **Verify:** offline new note + images → viewer opens; save → chip shows count.
- **2026-06-17** — Reconnect upload storm / stuck queued / no auto-retry on failed. **Symptom:** fast WiFi: all 3 stay "queued", Loading flickers, NPM activity repeats, restart fixes; slow 128kb/s: failures accumulate, manual Retry required. **Cause:** tight immediate re-flush loop; failed rows excluded from follow-up schedule; per-row refresh + `noteMediaChanged` spam; no server reconciliation after partial upload; fixed 90s timeout too short for throttled links. **Fix:** exponential backoff follow-up scheduler (includes failed, resets failed→pending each pass); reconcile queue with server before upload; batch refresh once per docId; silent panel refresh on sync events; dynamic upload timeout from blob size; 5s retry on 403 until note visible server-side. **Verify:** offline 3 images → online on fast and throttled links all sync without restart or manual retry.
- **2026-06-17** — Offline image reconnect: IDB wipe, duplicates, card preview glitches. **Symptom:** online all "failed"; reopen partial; chip over-counts; cache clear shows triplicates. **Cause:** `refreshRemoteNoteImages` wiped IDB previews on 403; flush re-uploaded without `remoteImageId` checkpoint; queued + remote double-counted. **Fix:** preserve local previews on 403; persist `remoteImageId`; `filterQueuedUploadsNotYetRemote`; blob recovery from preview row. Card layout issues were downstream of corrupt image hydration. **Verify:** offline 3 images → save → online without duplicates; chip matches panel.
- **2026-06-19 (re-applied)** — Bugs #140, #141, #143-144 re-implemented after prior attempt was reverted due to regressions. Key implementation notes for guards:
  - `refreshRemoteNoteImages` 403/404 with no preserved rows: only set `remoteCache.set(docId,[])` and return — do NOT call `syncRemotePreviewRows(docId,[])` or `emitNoteMediaChanged`. Wiping previews destroys queued upload thumbnails; emitting triggers a refresh → API → 403 → emit loop.
  - `isBlobUrl(url)` helper in `useResolvedNoteImageSource.resolveImmediateSource` — check before any offline/network branches; blob: URLs are always device-local.
  - `filterQueuedUploadsNotYetRemote(queuedRows, remoteImages)` in `noteMediaStore` — match by `remoteImageId` or `fileName:byteSize` key; used in chips and panel count so queued images that are already on the server don't double-count.
  - `flushQueuedNoteImages`: uses `isBrowserOffline()` (strict `navigator.onLine===false`), resets failed→pending at start of each pass, marks access errors (403) as pending+schedules 5s follow-up, other errors mark failed+schedules backoff follow-up.
  - `scheduleFlushFollowUp(userId, delayMs?)` — exponential backoff 2s→60s; cancel/replace any pending timer; resets after success.
  - `NoteMediaPanel` uses `scheduleQueuedNoteImageFlush` (not `flushQueuedNoteImages`); media-changed events use `refresh({silent:true})` to avoid Loading flicker.
  **Do not reintroduce:** wiping IDB previews on 403; counting all queued rows when already remote; direct `flushQueuedNoteImages` from panel online handler without debounce/backoff; calling `emitNoteMediaChanged` from 403 handler.
- **2026-06-20 (preview conditions + live drawing updates)** — Three note-card preview issues fixed:
  1. **Title condition:** `isMediaOnlyEmpty` previously required `!title.trim() && emptyContent`; changed to `emptyContent` only. Now any note with no body content shows image/drawing thumbnails even if it has a title. **Change:** removed `!title.trim() &&` from `isMediaOnlyEmpty` in `NoteCard.tsx`.
  2. **Linked drawing thumbnail as cropped square:** Previously used the same `drawingBody` CSS path as native drawing notes (`flex: 1 1 auto; min-height: 150px; object-fit: contain`). This caused card height oscillations whenever a thumbnail updated (the previous regression). **Fix:** split the JSX into two separate branches — native `type === 'drawing'` keeps existing `drawingBody`/`drawingThumbnail` (contain, variable height); `showLinkedDrawingPreview` uses new `linkedDrawingPreviewBody`/`linkedDrawingPreviewCell`/`linkedDrawingPreviewThumb` (padding-bottom 100% square trick + `object-fit: cover`). **Height is now completely stable** regardless of thumbnail content. **Do not reintroduce:** merging both paths back to shared CSS.
  3. **Live drawing thumbnail updates:** `useLinkedDrawingThumbnail` previously rendered once on mount. Added `drawingDoc.on('afterTransaction', observer)` after loading the doc; observer compares `getDrawingThumbnailVersion(doc)` and only schedules a re-render when the visual version key changes; debounced 500ms to avoid per-stroke jitter. Drawing docs sync via Yjs WebSocket, so this gives true cross-client live updates. **Verify:** edit a linked drawing on device A → card thumbnail on device B updates within ~1s of Yjs sync completing.
  - **Cross-client image updates:** not yet real-time (images are server-side DB records, not in Yjs). `useNoteCardImages` already refreshes from server on mount. True real-time image sync across devices requires server-push infrastructure (out of scope for this session).
  - **Warm-start & view-switch thumbnail pop-in (follow-up, same session):**
    - `useLinkedDrawingThumbnail`: initialize state from `peekLatestDrawingThumbnail(drawingId)` so linked drawing thumbnails appear synchronously on view-switch and warm-start (cached drawing thumbnail is already in the module-level `resolvedThumbnailCache`).
    - `useNoteCardImages`: initialize state from `getCachedRemoteNoteImages(resolvedDocId)` via the existing `buildNoteCardImagePreviewSources` + `mapNoteCardImagePreviews` helpers. Remote images have CDN URLs so no blob creation occurs during sync init. Fixes grid→list→grid and workspace-switch pop-in.
    - `startupHydration.ts`: added `readSynchronousWarmStartupMediaDocIds(limit=16)` — reads the render snapshot, returns `${workspaceId}:${noteId}` docIds for notes with `attachmentCounts.images > 0` (mirrors `DocumentManager.roomNameFor`).
    - `main.tsx`: added `preloadWarmStartupImages()` that calls `warmWorkspaceImageMetadata(docIds, { onlineRefreshLimit: 0 })` (IDB-only, no network) before `createRoot()`, raced against a 600ms timeout. Runs in `Promise.all` alongside banner preload. **Do not reintroduce** making this a sequential `await` before banners — both preloads must run in parallel.
    - `workspaceRenderSnapshot.ts` / `createSnapshotDocFromWorkspaceRenderSnapshot`: **drawingIds Y.Array was never populated** on snapshot docs. This caused `useLinkedDrawingIds` to return `[]` for snapshot-backed cards, `drawingSlots=0`, image cells in the wrong row, and a blank-then-appear flash when the live doc hydrated. **Fix:** push `note.drawingIds` into `doc.getArray<string>('drawingIds')` during snapshot doc creation. **Do not reintroduce** leaving this array empty — it breaks the grid layout from first render. **Verify:** warm start → media-only card with linked drawing shows drawing thumbnail in top row from frame 1 with no layout shift.
  - **Mixed drawing+image grid (follow-up, same session):** added `computeMediaPreviewSlots(drawingCount, imageCount)` — caps drawings at `DRAWING_PREVIEW_MAX=2`, fills rest with images up to `MEDIA_PREVIEW_MAX=4`. `useLinkedDrawingThumbnail` now takes an `index` parameter; called twice (slot 0, slot 1) unconditionally so React hook count is stable. JSX unified to a single `showMediaPreview` branch using the same `mediaImageBody`/`mediaImageGrid`/`mediaImageGridCell`/`mediaImageThumb` CSS. Old `showLinkedDrawingPreview`/`showImageGridPreview` kept as aliases (`= showMediaPreview`) so all height-effect conditions remain correct without rewrite. **Do not reintroduce:** separate JSX branches for linked drawings vs images; mixing them into the same stable-height grid is what prevents regressions.
- **2026-06-20** — Note banner warm-start pop-in / stale-removal bug. **Symptom:** (a) add a banner → close PWA → warm reopen → banner missing from first paint, appears after live doc loads; (b) remove a banner → close PWA → warm reopen → old banner shown on first paint, disappears after live doc loads. **Root causes:** (1) `assignNoteBannerFile` writes the banner to the Yjs doc metadata (shared-banner path) — the debounced render snapshot may not capture this before the app closes. (2) `readSynchronousWarmStartupBannerUrls` had an early exit `if (Object.keys(noteBannerPrefs).length === 0) return []` — shared banners are **not** stored in per-user `noteBannerPrefs`, so this guard wrongly skipped all banner preloading for shared-banner users. (3) `createSnapshotDocFromWorkspaceRenderSnapshot` only read `note.hasSharedBannerPreference` from the stale snapshot — a recently-added banner whose snapshot hadn't saved showed no banner in the snapshot doc. **Fix:** Created `src/core/noteBannerWarmCache.ts` (`fn_banner_warm_v1` in localStorage) that stores `noteId → bannerFile|null` synchronously. `writeNoteBannerWarmCacheFile(noteId, fileName)` is called at every `assignNoteBannerFile` call site (NoteGrid banner picker, NoteEditor, DrawingEditor). `readSynchronousWarmStartupBannerUrls`: gate is `if (Object.keys(noteBannerPrefs).length === 0) return []` — **the original gate, untouched**. The warm cache is read after the gate only to extend legacy-prefs preloading with more-recent picks; it must NEVER open the gate by itself. The warm cache persists across sessions, so a non-empty warm cache would otherwise block createRoot() on every warm start after the user ever changed a banner (destroying drag animations, drawing thumbnail timing, and all startup performance). The snapshot-doc fix handles first-paint correctness without blocking startup. `createSnapshotDocFromWorkspaceRenderSnapshot` reads `getNoteBannerWarmCacheEntry(note.id)`: warm string → set `metadata.bannerFile`; warm null → don't set key (no banner, `hasSharedNoteBannerPreference` stays false); undefined → fall through to existing render-snapshot logic. Also fixed a pre-existing bug: `assignNoteBannerFile` was used but never imported in `DrawingEditor.tsx`. **Critical do-not-reintroduce:** never change `readSynchronousWarmStartupBannerUrls` to unconditionally process shared banners — it gates createRoot() on up to 900ms of image loading and breaks startup timing (drag animations, title/chip pop-in, drawing thumbnails). **Verify:** add banner → immediately close PWA → warm reopen → banner appears on first paint. Remove banner → immediately close PWA → warm reopen → no banner on first paint.
- **2026-06-20** — Chip row gate in `renderNoteMetaChips` never mounted the chip for offline-only queued images. **Cause:** `liveAttachmentTotal` used only `getCachedRemoteNoteImages()` (synchronous in-memory remote cache) and `attachmentShellTotal` (server snapshot) — neither knows about IDB-queued uploads, so the early-return fired and `NoteAttachmentCountChip` never mounted. Even though the chip handles its own async count refresh, it can't update what it never mounts. **Fix:** added `queuedCountByDocId` (synchronous module-level Map) to `noteMediaStore`; incremented in `queueNoteImagesForUpload`, decremented after successful flush or `deleteQueuedNoteImage`; exported `getQueuedNoteImageCount(docId)` read by `liveAttachmentTotal` in `NoteGrid`; hydrated at startup via `warmQueuedImageCounts(userId, docIds)` called alongside `warmWorkspaceImageMetadata`. **Verify:** go offline → create note → add images → save → chip appears immediately on note card without network.
- **2026-06-17** — Media-only card thumbnails loaded eagerly for every mounted card. **Symptom:** image/drawing-only cards pop in after the grid paints; off-screen cards still hit IDB + drawing render. **Fix:** `useNoteCardMediaViewport` (IntersectionObserver, 360px root margin) gates off-screen IDB/render work; reserve media-only shell via sync attachment counts + skeleton cells. **Warm reopen images:** preload image docIds + decode in `main.tsx` bootstrap. **Warm reopen linked drawings (2026-06-17):** snapshot Y.Docs from `createSnapshotDocFromWorkspaceRenderSnapshot` omitted `drawingIds`, so `useLinkedDrawingIds` returned `[]` until live Yjs hydrated — card rendered without drawing body, then resized when live doc arrived. **Fix:** populate `drawingIds` on snapshot docs; pass `initialLinkedDrawingIds` from render snapshot shell; `useLinkedDrawingThumbnail` syncs `peekLatestDrawingThumbnail` whenever `firstDrawingId` is known (including while viewport IO has not fired). **Realtime (2026-06-17):** collaborator image/drawing changes did not update media-only cards. **Fix:** `useNoteCardImages` always listens for `emitNoteMediaChanged` and force-refreshes remote images; `useNoteCardImageAttachmentCount` syncs from remote cache; linked drawings subscribe to drawing doc `afterTransaction` via `useRemoteDrawingDocVersion`. **Verify:** warm reopen — media-only cards with linked drawing show thumbnail on first paint; collaborator add/remove image or edit drawing updates card without reload.